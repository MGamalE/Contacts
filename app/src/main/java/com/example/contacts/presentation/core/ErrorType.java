package com.example.contacts.presentation.core;

public enum ErrorType {
    GENERIC_ERROR,
    UNAUTHORIZED_ERROR,
    FORBIDDEN_ERROR,
    NOT_FOUND_ERROR,
    SERVER_ERROR,
    UNEXPECTED_ERROR,
    CONNECTION_ERROR
}
